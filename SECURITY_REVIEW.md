# LanZ Monitor 安全审查报告

审查日期：2026-09-01
审查版本：v1.4.3 本地候选版本
审查范围：`LanZMonitor.ps1`、`Set-LanZSession.ps1`、`Build-Exe.ps1`、运行时数据目录、WebView2 会话、GitHub Release 更新链路

## 结论

本轮未发现会将账号密码、会话 Cookie 或请求密钥提交到仓库的代码，也未发现远程执行网页内容、绕过 TLS 证书验证或静默下载并运行未知文件的行为。

现有架构适合作为单用户本地监控工具：有效会话由 Windows DPAPI 以当前用户范围加密；模型状态和当日额度来自真实 WebView2 页面流量；计费规则采用低频同步；本地历史和日志都有保留上限。

当前主要剩余风险是发布 EXE 尚未使用代码签名证书，以及内部服务的兼容网络客户端关闭了证书吊销列表查询。前者建议在项目进入稳定发布阶段后解决；后者没有关闭主机名、有效期和证书链验证，并且不会被 GitHub 更新客户端复用。

## 数据与凭据边界

| 数据 | 存储位置 | 保护方式 | 是否进入仓库 |
| --- | --- | --- | --- |
| 会话值 | `%LOCALAPPDATA%\LanZ-Monitor\secrets\session.bin` | DPAPI `CurrentUser` | 否 |
| WebView2 登录状态 | 独立 WebView2 用户目录 | Windows 用户权限及 WebView2 自身保护 | 否 |
| UI 偏好、规则缓存 | `%LOCALAPPDATA%\LanZ-Monitor\settings` | 当前用户文件权限 | 否 |
| 负载历史与诊断日志 | `%LOCALAPPDATA%\LanZ-Monitor\data` | 当前用户文件权限、容量/时间限制 | 否 |

程序不会读取网页登录密码。WebView2 可以在用户明确同意时使用自身的密码保存能力，但监控程序只读取内置 LanZ 域名设置的指定会话 Cookie。

LanZ 的公开页面地址、响应路径、Cookie 名称和接口状态码作为只读协议配置内置。首次运行直接复用“重新登录”WebView2 流程，自动取得并以 DPAPI `CurrentUser` 保存有效会话；用户不填写 API、Cookie、AES 或接口状态码。旧版 `connection.bin` 不再参与运行。

## 发现与处置

### SEC-01：Release 自更新供应链风险 — 中风险，已缓解

风险：如果更新器只相信文件名或下载地址，仓库附件被替换、错误重定向或网络异常都可能让未知 EXE 进入执行链路。

处置：

- 只允许访问固定仓库 `HankLiu2020/LanZ-Monitor`。
- 只跟随 GitHub 标记为 Latest 的 Release 固定下载入口，不接受用户提供的更新源。
- `latest.txt` 只允许 `version`、`file`、`size`、`sha256` 四个唯一字段，并且限制为 4 KB。
- 只接受与清单版本完全匹配的 `LanZ-Monitor-v<版本>.exe`。
- 校验 HTTPS 主机、Release 路径、版本格式和 100 KB–64 MB 文件范围。
- 下载后必须匹配 `latest.txt` 中的 SHA-256；摘要缺失或格式异常时禁止自动安装。
- 更新文件先写入 `%LOCALAPPDATA%\LanZ-Monitor\updates`，不直接覆盖运行中的 EXE。
- 用户再次确认后才退出程序；替换前会再次计算暂存文件 SHA-256，替换或新版本启动失败时恢复旧文件。
- GitHub 更新客户端独立创建，并保持证书吊销检查开启。
- 检查更新仅由用户点击触发，不增加后台遥测。

`latest.txt` 与版本化 EXE 作为同一个 Release 的附件发布；构建脚本根据最终 EXE 自动写入文件名、字节数和 SHA-256，避免人工抄写。

### SEC-02：发布文件未代码签名 — 中风险，未解决

影响：Windows SmartScreen 可能警告；SHA-256 能验证下载与 GitHub 元数据一致，但不能替代受信任发布者签名。

建议：以后使用受保护的代码签名证书签署 EXE，并在发布流水线中验证签名。签名私钥不得放入仓库或普通构建目录。

### SEC-03：内部服务客户端不查询 CRL — 中风险，已隔离

原因：当前内部 TLS 环境无法稳定取得证书吊销列表，强制查询会拒绝所有内部请求。

边界：代码没有安装自定义证书验证回调，主机名、证书有效期和证书链仍由 .NET 验证。GitHub 更新使用独立客户端并显式启用 CRL 查询。

建议：如果内部 PKI 后续提供可访问的 OCSP/CRL，应重新开启内部请求的吊销检查。

### SEC-04：诊断日志可能持续增长 — 低风险，已修复

`browser-diag.log` 达到 1 MB、`login-diag.log` 达到 2 MB 后会轮转为单个 `.old` 文件。更新日志同样限制为约 256 KB。负载归档原有 30 天和 50,000 行上限保持不变。

### SEC-05：登录诊断记录过多 Cookie 元数据 — 低风险，已修复

诊断日志不再列出全部 Cookie 名称、域名和路径，也不记录配置的 Cookie 名称。现在只记录 Cookie 数量、是否出现目标会话以及值长度/是否为无效占位值；从不记录 Cookie 值、Token、请求头或响应正文。

### SEC-06：页面结构与规则解析变化 — 低风险，已有降级

网页或接口字段变化可能造成展示错误，但解析失败不会执行页面内容，也不会修改凭据。模型说明解析失败时留空；计费规则读取失败时使用最近一次缓存并显示来源状态。

## 网络行为

- 模型负载：由持久 WebView2 打开真实页面并被动解析页面自身周期性响应。
- 用量信息：低频同步，当前间隔 120 秒。
- 计费规则：最多每 4 小时重新抓取一次；当前时段使用缓存规则和本地时间重算，不产生网络请求。
- GitHub 更新：只有用户点击“检查更新”时读取一次 Latest Release 的 `latest.txt`；发现更高版本后自动下载并校验，退出替换前仍需用户确认。

2026-08-31 实测确认 GitHub 未认证 REST API 在共享出口 IP `223.119.45.20` 上已耗尽每小时 60 次额度，即使合法 UA 也返回 HTTP 403、`X-RateLimit-Remaining: 0`。因此 v1.4.1 候选版不再调用 `api.github.com`，改用 GitHub 官方 `releases/latest/download/latest.txt` 重定向地址；这与浏览器打开 Release 页一样不消耗 REST API 配额。

## 验证记录

- 主脚本 PowerShell AST 解析通过。
- 更新检查和下载两个后台脚本分别通过独立语法解析。
- 使用已发布 v1.4.0 EXE 进行真实下载测试：大小 `1,147,392` 字节，SHA-256 为 `2A9E017EFB0EAAF7852DBC5BD27572D08E88BB9B56025A9B21B3978DA4033FFE`，验证通过。
- 使用故意错误的 SHA-256 进行反向测试：下载物被拒绝，没有生成 `.ready` 可安装文件。
- 在 `.build` 一次性副本中完成端到端自更新测试：旧版退出、安装前二次 SHA-256 校验、EXE 替换及 v1.4.1 重启均成功；测试后已恢复原托盘实例并删除一次性副本。
- 构建生成的 `latest.txt` 与最终 EXE 的版本、文件名、字节数和 SHA-256 逐项一致；更新清单和下载后台脚本均通过独立语法解析。
- 固定 Latest 清单地址实测从 `/releases/latest/download/latest.txt` 重定向到 `/releases/download/v1.4.0/latest.txt`，响应不包含 REST API 限额头；当前线上 404 是因为尚未发布该清单，v1.4.1 发布时需与 EXE 同时上传。
- v1.4.2 将手动刷新对齐为用户触发的一次完整浏览器刷新：等待资源看板产生新的用量响应后再取得模型负载，同时强制同步计费规则；自动刷新与后台低频规则同步节奏不变。
- v1.4.3 将隐藏 WebView2 的跨域 SSO 跳转、服务域 `/login` 跳转及模型/额度接口 401/403 统一视为会话过期，并在 UI 调度器中防抖弹出内置登录窗；不会循环弹窗，也不会清除仍可复用的 SSO 状态。
- v1.4.3 同时以显式托盘可见状态取代对 WPF `IsVisible` 瞬时值的单独依赖，并对重复双击事件防抖；浏览器捕获计时器由 120ms 调整为 500ms，仍远快于网页约 3 秒一次的模型状态包，可降低长期驻留时的 UI 线程压力。
- v1.4.3 修正图表历史只压缩磁盘文件、却让内存原始采样无限增长的问题：每次持久化后以内存中的同一份降采样结果继续运行，并把历史文件写入限制为最多每 10 秒一次；页面响应仍全部接收，图表按既定 10 秒粒度呈现。
- v1.4.3 继续监听网页约 3 秒一次的正常状态包，但只按图表既定的 10 秒粒度刷新 WPF 卡片和历史图形；内存中的最新响应不会丢失，手动刷新与首次捕获仍即时显示，以避免无意义的高频整表重绘。
- v1.4.3 启动时立即压缩旧版遗留的密集采样。窗口隐藏在托盘时只更新内存、压缩历史和本地快照，不再修改不可见的 WPF 数据源；双击恢复先显示窗口，再延迟 250ms 用最新状态重绘一次，避免恢复动作被积压布局阻塞。
- 使用临时测试构建直接调用托盘“退出”菜单项的同一 Click 事件链，回调与退出诊断均成功写入，进程以退出码 0 结束；测试开关随后从正式源码移除。
- 临时移走现有 `session.bin` 与旧版 `connection.bin` 后真实启动候选 EXE，程序只弹出“LanZ 登录”窗口并直接加载统一登录页；界面不包含 API、Cookie 名称、AES 或状态码输入。关闭登录页后进程按托盘模式继续运行，测试结束后原加密会话与旧配置均已恢复且未遗留测试凭据。
- 仓库按 LanZ 专用方案保留公开服务域名、响应路径和 Cookie 名称；扫描未发现有效会话值、账号密码或实际请求签名密钥。

## 后续建议

1. 为正式 EXE 增加 Authenticode 代码签名。
2. 在 GitHub Release 发布流程中同时上传构建生成的 `latest.txt`，禁止手工修改其中的摘要。
3. 将应用版本号收敛为单一构建来源，避免脚本版本与 EXE 文件版本漂移。
4. 每次修改登录、网络或更新代码后，重复正确摘要和错误摘要两条测试。
